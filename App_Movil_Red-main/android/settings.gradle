rootProject.name = 'red-social-expo'

apply from: new File(["node", "--print", "require.resolve('expo/package.json')"].execute(null, rootDir).text.trim(), "../scripts/autolinking.gradle");
useExpoModules()

apply from: new File(["node", "--print", "require.resolve('@react-native-community/cli-platform-android/package.json')"].execute(null, rootDir).text.trim(), "../native_modules.gradle");
applyNativeModulesSettingsGradle(settings)

include ':app'
// Re-enable local composite build for react-native-gradle-plugin to compile the
// plugin from the local `node_modules` sources. This is a temporary step for
// reproducing and fixing the Kotlin metadata issue on this machine. If you
// prefer resolving via published artifact, revert this change and ensure the
// top-level `build.gradle` declares a matching classpath version.
includeBuild(new File(["node", "--print", "require.resolve('react-native-gradle-plugin/package.json')"].execute(null, rootDir).text.trim()).getParentFile())
