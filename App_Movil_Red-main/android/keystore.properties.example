## Plantilla de ejemplo para `keystore.properties`
# No incluyas este archivo con valores reales en repositorios públicos.
# Renombra a `keystore.properties` y completa con tus datos reales si lo usas localmente.

# Ruta al archivo .jks que contiene tu keystore
storeFile=/ruta/a/mi_keystore.jks

# Contraseña del keystore
storePassword=TU_STORE_PASSWORD

# Alias de la llave dentro del keystore
keyAlias=TU_ALIAS

# Contraseña de la clave (si es distinta de la storePassword)
keyPassword=TU_KEY_PASSWORD
